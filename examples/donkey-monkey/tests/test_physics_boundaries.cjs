const assert=require('node:assert/strict'),path=require('node:path');const root=path.resolve(__dirname,'..');const {levels}=require(path.join(root,'levels.js'));const {movePlayer}=require(path.join(root,'physics.js'));const l=levels[0],lad=l.ladders[0],g={x:lad.x+lad.w/2-12,y:lad.y-32,vx:0,vy:0,onGround:true};const initial=g.y;for(let i=0;i<15;i++)movePlayer(g,l,{down:true},1/60);console.log(JSON.stringify({initial,after:g,ladder:lad}));assert.ok(g.y>initial+15,'ArrowDown must enter ladder from supported top platform');
const fs=require('node:fs'),path=require('node:path'),vm=require('node:vm'),assert=require('node:assert/strict');const root=path.resolve(__dirname,'..');const source=fs.readFileSync(path.join(root,'game.js'),'utf8');for(const values of [[0.1,0.9],[0.9,0.1],[0.1,0.1],[0.9,0.9]]){let n=0;const math=Object.create(Math);math.random=()=>values[n++%values.length];const scope={module:{exports:{}},require:p=>require(path.resolve(root,p)),Math:math};vm.runInNewContext(source,scope);const api=scope.module.exports,g=api.createGame();for(let i=0;i<360;i++)api.update(g,{},1/60);console.log(JSON.stringify({values,barrels:g.barrels.length,lives:g.lives}));assert.ok(g.barrels.length>0,'Both spawn sides must produce on-screen rolling hazards at six seconds');assert.equal(g.lives,3,'Initial player must remain safe');}